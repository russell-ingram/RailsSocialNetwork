

<div class="myConnection">
  <div class="myConnWrapper">
    <div class="myConnProfilePic"><a href="/profile/<%= friend.friend.id %>">
    <% if friend.friend.profile_pic_url? %>
      <%= image_tag friend.friend.profile_pic_url %>
    <% else %>
      <%= image_tag("avatar-icons/avatar_0"+rand(1..9).to_s+".svg")  %>
    <% end %>
    </a></div>

    <div class="myConnInfo">
      <div class="myConnName"><%= link_to friend.friend.full_name, user_profile_path(friend.friend) %></div>
      <% friend_pos = friend.friend.current_pos %>
      <div class="myConnCompany"><%= friend_pos["company"] %></div>
      <div class="myConnIndustry"><%= friend_pos["job_title"] %>, <%= friend_pos["industry"] %>, <%= friend_pos["enterprise_size"] %></div>
      <a href="/messages/new/<%= friend.friend.id %>" class="break-2-show-inline messageLink blue-link">MESSAGE<span class="icon icon-fwd-arrow"></span></a>
    </div>
    <div class="myConnMessage break-2-hide"><a href="/messages/new/<%= friend.friend.id %>" class="blue-link">MESSAGE<div class="icon icon-fwd-arrow"></div></a></div>

    
  </div>
</div>
